import React from 'react'
import { connect } from 'react-redux'

const AboutView = ({ dispatch }) => (
    <main>
        <h1>A little bit about me</h1>


    </main>
)

export default connect()(AboutView)